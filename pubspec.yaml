name: etf_rank_app
description: 宽基ETF轮动 - A-share ETF momentum ranking app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.2.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  sqflite: ^2.3.0
  path_provider: ^2.1.0
  path: ^1.9.0
  intl: ^0.19.0
  provider: ^6.1.0
  charset: ^2.0.1
  fl_chart: ^1.2.0
  shared_preferences: ">=2.5.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
